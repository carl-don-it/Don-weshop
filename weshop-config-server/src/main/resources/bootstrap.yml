server:
    port: 8000

spring:
    application:
        name: weshop-config-server
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/Walker-Don/Don-weshop
                    searchPaths: config-repo
                    username: username
                    password: password


